pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh 'docker build -t testapp server/.'
                sh 'docker run testapp bash -c "cd handlers/ && go test"'
                sh 'docker rmi --force $(docker images -q testapp | uniq)'
            }
        }

        stage('Deploying') {
            steps {
                sh 'docker-compose up -d --build app'
            }
        }
    }
}
